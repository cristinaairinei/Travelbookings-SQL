
## TravelBookings Database

University project developed using SQL Server.

The project models a travel booking system and includes:
- Relational database schema with multiple entities and relationships
- Sample data inserts
- Complex queries using joins, subqueries, group by and having
- Stored procedures, user-defined functions and views
- Triggers and indexes

The project was used to practice database design and SQL querying.
